extends layout

block content
    h4 Select server：
    each server in servers
        div(class=["checkbox", "server"])
            label
                input(type="checkbox", value="#{server}" checked=true)
                span #{server}
    h4 Select item：
    each v, k in indexs
        div(class=["checkbox", "index"])
            label
                - var isCMDPS = k === 'cmd_ps'
                input(type="checkbox", value="#{k}" checked=isCMDPS)
                span #{v}
    h4 Select the aggregation：
    each v, k in ways
        div(class="radio")
            label
                - var isDefault = k === 'default'
                input(type="radio", name="reduce_way", value="#{k}" checked=isDefault)
                span #{v}
    h4 Select a time range:：
    div(class="form-inline")
        div(class=["input-group", "date"], id="begin_datetime")
            input(type="text", class="form-control", name="begin-datetime")
            span(class="input-group-addon")
                span(class=["glyphicon", "glyphicon-calendar"])
        div(class=["input-group", "date"], id="end_datetime")
            input(type="text", class="form-control", name="end-datetime")
            span(class="input-group-addon")
                span(class=["glyphicon", "glyphicon-calendar"])
        button(type="submit", class=["btn", "btn-primary"], id="show_stat") Submit

    div(class="stat-graph-part")
